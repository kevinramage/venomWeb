name: Navigation
vars:
  web:
    driver: {{browser}}
    port: 8888
testcases:
- name: Navigation on specific driver port
  steps:
  - type: web
    action:
      navigate:
        url: https://www.google.fr