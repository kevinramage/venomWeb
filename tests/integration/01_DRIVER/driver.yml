name: Navigation
vars:
  web:
    driver: {{browser}}
    driverBinary: ""
testcases:
- name: Navigation with specific driver binary
  steps:
  - type: web
    action:
      navigate:
        url: https://www.google.fr